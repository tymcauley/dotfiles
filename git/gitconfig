[user]
    email = 16469394+tymcauley@users.noreply.github.com
    name = Tynan McAuley
[push]
    # When you use 'git push', push only the current branch, not all branches.
    default = simple
    recurseSubmodules = check
[diff]
    tool = icdiff
[difftool]
    prompt = false
[difftool "icdiff"]
    cmd = icdiff --line-numbers $LOCAL $REMOTE
[pager]
    difftool = true
[merge]
    tool = vimdiff
[alias]
    privateprofile = config user.email \"16469394+tymcauley@users.noreply.github.com\"
    dt = difftool
    diffws = diff --ignore-space-at-eol --ignore-space-change --ignore-all-space --ignore-blank-lines
    showtool = "!f() { git difftool $1^ $1; }; f"
    added = difftool --cached
